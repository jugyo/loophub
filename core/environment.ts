// The current workspace branch. Commands that create workspace-scoped domain objects may use this
// as their default while still allowing an explicit command-line option to take precedence.
export const ENV_WORKSPACE = "LOOPHUB_WORKSPACE";
